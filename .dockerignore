# VCS / CI
.git
.github

# Dependencies & build output (reinstalled / regenerated in the image)
node_modules
dist
dist-ssr
coverage
.vite
.cache

# Local agent / IDE artifacts
.cursor
.claude
.vscode
.idea
terminals
agent-transcripts
mcps

# Logs
*.log

# Secrets & private host data — never bake into the image
.env
.env.*
*.pem
*.key
map.json
*.local.json
ros
params

# Docs / compose (not needed inside the image)
*.md
docker-compose*.yml
