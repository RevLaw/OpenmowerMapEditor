services:
  openmower-map-editor:
    build: .
    container_name: openmower-map-editor
    ports:
      - "8080:80"
    volumes:
      - type: bind
        source: ./params
        target: /data/params
        read_only: true
      - type: bind
        source: ./ros
        target: /data/ros
